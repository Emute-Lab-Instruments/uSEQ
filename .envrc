# direnv configuration for uSEQ development

# Desktop development (Meson builds)
use nix

# Uncomment the following line to use PlatformIO environment instead:
# use nix platformio.nix

# Load local environment variables if they exist
if [ -f .envrc.local ]; then
  source_env .envrc.local
fi