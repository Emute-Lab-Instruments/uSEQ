// test_io_manager.cpp - IO Manager Test Suite
#include <doctest.h>

TEST_SUITE("IOManager") {
    TEST_CASE("IOManager placeholder test") {
        // Placeholder test to allow build to complete
        CHECK(true);
    }
}